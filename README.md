PHP Backporter
==============

Currently supported
-------------------

* Conversion of `const` statements into `define` function calls
* Conversion of `__DIR__` to `dirname(__FILE__)`

ToDo
----

* Namespaces
* Lambda functions
* Closures